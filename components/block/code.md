```
  render() {
    var styles = {
      self: {
        background: 'rgba(0,0,255,0.2)',
        border: '5px solid rgba(0,0,255,0.1)'
      }
    };

    return (
      <Block width={0} pad={true} styles={styles} />
    );
  }
```